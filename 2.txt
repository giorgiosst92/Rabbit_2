Run this code with python3, to install and run use the following commands:
pip3 install -r requirements.txt
python3 rabbit.py


Note: change the value stored inside [processing].[a/b/c] in the config.ini file in order
to test different scenarios


At the end you will get a pint in the console telling:

Minimum number of jumps to reach end is 3